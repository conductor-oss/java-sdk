{
  "name": "wf_timeout_long",
  "description": "Workflow with a 3600-second (1 hour) timeout. Uses a fast worker that completes immediately.",
  "version": 1,
  "schemaVersion": 2,
  "inputParameters": ["mode"],
  "tasks": [
    {
      "name": "wft_fast",
      "taskReferenceName": "wft_fast_ref",
      "type": "SIMPLE",
      "inputParameters": {
        "mode": "${workflow.input.mode}"
      }
    }
  ],
  "outputParameters": {
    "result": "${wft_fast_ref.output.result}"
  },
  "ownerEmail": "examples@orkes.io",
  "timeoutSeconds": 3600
}
